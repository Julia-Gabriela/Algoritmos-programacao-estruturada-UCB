#include <stdio.h>
///////////////////VETORES///////////////////
int main()
{
    // tipo \ nome\ tamanho
    /*
        int nota[3];
        double media;

        nota[0] = 8;
        nota[1] = 9;
        nota[2] = 7;

        media = (nota[0] + nota[1] + nota[2])/3;
        printf("Soma = %.2lf", media );
    */
    /*int nota[3] = {9, 7, 8};
        double media;
        int i;

        for (i = 0; i < 3; i++)
        {
            media += nota[i];
        }

        printf("Soma = %.2lf", media / 3);
    */
    /*
        int nota[3];
        double media;
        int i;

        for(i = 0; i < 3; i++){
        printf("Digite a nota %d ", i + 1);
        scanf("%d", &nota[i]);
        } 

       for(i=0; i<3; i++){
        printf("Notas Digitadas:\n indice - %d \n nota - %d \n",i, nota[i]);
       }*/
}
